services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bleep-backend-lab1
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - bleep_data:/app/data
    restart: unless-stopped

volumes:
  bleep_data:
